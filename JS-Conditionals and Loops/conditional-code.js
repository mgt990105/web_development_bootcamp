const myName = "Mx";

if (myName === "Max") {
  console.log("Hello!");
}

let isLoggedIn = false;

if (isLoggedIn) {
  console.log("User is logged in");
}

// const enteredUserName = "Mandula"
const enteredUserName = "d";

// JavaScript treats empty strings and zeroes as false.
// That's how the below example works.

if (enteredUserName) {
  console.log("Input is valid!");
}
