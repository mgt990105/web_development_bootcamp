var image = document.getElementsByClassName("parallax-image");
new simpleParallax(image, {
  scale: 1.6, // Adjust the scale as desired
  orientation: "up", // Choose the direction of parallax effect
  delay: 0.2, // Optional, adds a delay to the effect
});
