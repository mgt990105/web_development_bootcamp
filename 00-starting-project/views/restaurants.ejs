<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head.ejs',{}) %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <%- include('includes/header.ejs',{}) %>
    <%-include('includes/side-drawer.ejs',{}) %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if (numberOfRestaurants === 0) { %>
      <p>Unfortunately, we have no restaurants yet - please recommend some!</p>

      <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We found <%= numberOfRestaurants %> restaurants.</p>
      <% } %>
      <ul id="restaurants-list">
        <!-- we are writing JavaScript code -->
        <% for (const restaurant of restaurants) { %>
        <!-- the left restaurant is the key name from the other file and the right is the loop item -->
        <%-include('includes/restaurants/restaurant-item',{restaurant:restaurant})%>
        <% }%>
      </ul>
    </main>
  </body>
</html>
